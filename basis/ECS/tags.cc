#include <basis/ECS/tags.h>  // IWYU pragma: associated

ECS_DEFINE_METATYPE(UnusedTag)

ECS_DEFINE_METATYPE(NeedToDestroyTag)

ECS_DEFINE_METATYPE(DelayedConstruction)

ECS_DEFINE_METATYPE(DelayedConstructionJustDone)
